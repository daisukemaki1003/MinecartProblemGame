import '../../entity/model/event.dart';

class EventOutputData {
  List<Event> events;
  EventOutputData(this.events);
}

class EventLoadOutputData {
  List<Event> events;
  EventLoadOutputData(this.events);
}
